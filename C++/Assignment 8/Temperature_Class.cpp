/*#include "Temperature.h"

float Temperature::getCelsius() const{
    float f = temp;
    float c;

    c = (f - 32) * (5/9);
    return c;
}

float Temperature::getKelvin() const{
    float f = temp;
    float k;

    k = (f - 32) * (5/9) + 273.15;
    return k;
}
*/ 